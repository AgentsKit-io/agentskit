/**
 * Re-export of the Ask streaming protocol + generative-UI tool defs, now in
 * `@agentskit/ask-core` (RFC-0007 F0). Thin shim so existing `@/lib/ask/protocol`
 * imports across the widget + route keep working.
 */
export * from '@agentskit/ask-core'
