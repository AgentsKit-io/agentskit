# Changelog

## 0.16.0

### Minor Changes

- Add Node-only CLI text, JSON, and ACP v1 transports with explicit safety modes, deadlines, cancellation, bounded output, fail-closed structured responses, and first-party provider manifests.
- Add request-aware argv builders, raw-output decoding, and bounded provider output-file support.
- Harden configurable CLI adapter transports, provider manifests, deadlines, cancellation, and output handling.
