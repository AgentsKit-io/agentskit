1/ First agent without a provider key

Why it exists: New developers need a zero-key path to feel the AgentsKit runtime contract.

2/ The path:
1. `npm install @agentskit/core @agentskit/runtime tsx`
2. `cp apps/docs-next/fixtures/first-agent/agent.ts ./agent.ts`
3. `npx tsx agent.ts`

3/ Claims (generated, not hand-typed):
- 25 packages
- 7 framework bindings

4/ Next products:
- AgentsKit: Build agents without gluing many libraries together.
- AgentsKit Registry: Copy ready-made agents and own the source.
- AgentsKit Chat: Define one agent experience and deliver it across interfaces.

5/ This thread is a draft atom. Publishing requires explicit human approval.
