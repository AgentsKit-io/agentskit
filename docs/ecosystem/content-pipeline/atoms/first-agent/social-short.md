First agent without a provider key.

Three commands. No provider key.

```bash
npx tsx agent.ts
```

Built on AgentsKit (25 packages, 7 framework bindings).

Docs + fixture linked in the atom. Human-reviewed drafts only.
