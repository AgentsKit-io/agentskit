Swap the model provider without rewriting the agent.

Three commands. No provider key.

```bash
AGENT_PROVIDER=ollama npx tsx agent.ts
```

Built on AgentsKit (25 native adapters, 140 providers).

Docs + fixture linked in the atom. Human-reviewed drafts only.
