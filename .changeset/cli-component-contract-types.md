---
---

Internal: add the UI component registry contract types (`packages/cli/src/components/types.ts`, RFC-0006) — `RegistryComponent`, `ComponentPort`, `PortServer`, `RegistryFile`, `FrameworkTarget`, `ProjectScan`, `ComponentsConfig`, audit/marker/signing-key shapes. Not exported from the package entry yet, so there is no public API change and no release.
