---
'@agentskit/integrations': minor
---

Add more catalog-native service integrations: **calendly** (me / list event types), **dropbox** (list / create folder), **box** (list items / create folder), and **baserow** (list / create rows). Covers Bearer and `Token`-scheme auth.
