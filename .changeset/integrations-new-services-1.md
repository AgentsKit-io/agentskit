---
'@agentskit/integrations': minor
---

Add new service integrations to the catalog: **telegram** (bot `sendMessage` / `sendPhoto`) and **sendgrid** (v3 Mail Send). Both are catalog-native (no `@agentskit/tools` shim) and follow the unified `Integration` contract.
