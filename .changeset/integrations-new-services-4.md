---
'@agentskit/integrations': minor
---

Add more catalog-native service integrations: **google-drive** (list files / create folder, OAuth2), **assemblyai** (submit / fetch transcript), **attio** (query / create records), and **apollo** (people search). Covers OAuth2 Bearer, raw-key Authorization, and `X-Api-Key` header auth.
