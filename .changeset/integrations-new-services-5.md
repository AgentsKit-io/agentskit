---
'@agentskit/integrations': minor
---

Add more catalog-native service integrations: **whatsapp** (Cloud API send), **bigcommerce** (products / orders), **salesforce** (SOQL query / create record), **azure-openai** (chat completion), **mailchimp** (add member / list audiences), and **acuity** (appointments / types). Covers Bearer, custom token headers, OAuth2 per-instance, `api-key` header, and HTTP Basic auth.
