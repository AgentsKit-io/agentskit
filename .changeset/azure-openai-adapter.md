---
'@agentskit/adapters': minor
---

feat(adapters): add `azureOpenAI` adapter — Azure OpenAI Service with deployment-name routing, pinned `api-version`, and `api-key` header auth. Default api-version `2024-10-21`. Capabilities: streaming, tools, usage.
