---
'@agentskit/adapters': minor
---

Add the Node-only `@agentskit/adapters/cli` subpath with shell-free text, JSON,
and ACP v1 CLI transports, explicit safety modes, deadlines, cancellation,
bounded output, fail-closed structured responses, and validated first-party
provider manifests for Codex, Claude Code, Grok CLI, and OpenCode.
