---
"@agentskit/cli": patch
---

**cli**: `agentskit init` now prints a star-CTA after scaffolding (both the
interactive `printNextSteps` flow and the non-interactive CI path), pointing
users to the GitHub repo. No behavior change to the generated project.
