---
'@agentskit/adapters': minor
---

feat(adapters): firm up `cohere` adapter — explicit `capabilities: { streaming, tools, usage }`, default model `command-r-plus`, dedicated test suite mirroring `anthropic.test.ts`, and a `cohereAdapter` alias for naming consistency.
